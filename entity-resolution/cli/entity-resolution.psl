// Similar names implies the same author
1.0: AuthorName(A1,N1) & AuthorName(A2,N2) & SimName(N1,N2) >> SameAuthor(A1,A2) ^2

// Similar titles implies the same paper
1.0: PaperTitle(P1,T1) & PaperTitle(P2,T2) & SimTitle(T1,T2) >> SamePaper(P1,P2) ^2

// If two references share a common publication and have the same initials, then this implies the same author
1.0: AuthorOf(A1,P1) & AuthorOf(A2,P2) & SamePaper(P1,P2) & AuthorName(A1,N1) & AuthorName(A2,N2) & SameInitials(N1,N2) >> SameAuthor(A1,A2) ^2

// If two papers have a common set of authors and the same number of tokens in the title, then this implies the same paper
1.0: SameAuthorSet(P1,P2) & PaperTitle(P1,T1) & PaperTitle(P2,T2) & SameNumTokens(T1,T2) >> SamePaper(P1,P2) ^2

// Priors: By default, Authors are not the same Authors and Papers are not the same Papers
1.0: !SameAuthor(A1,A2) ^2
1.0: !SamePaper(A1,A2) ^2
